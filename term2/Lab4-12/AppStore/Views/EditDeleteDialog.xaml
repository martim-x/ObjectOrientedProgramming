<Window
  x:Class="Project.Views.EditDeleteDialog"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Background="{DynamicResource WindowBgBrush}"
  FontFamily="Segoe UI"
  Width="300"
  SizeToContent="Height"
  WindowStartupLocation="CenterOwner"
  ResizeMode="NoResize"
>
  <Border CornerRadius="14" Background="{DynamicResource WindowBgBrush}" BorderThickness="0">
    <Border.Effect>
      <DropShadowEffect ShadowDepth="0" BlurRadius="28" Opacity="0.16" Color="#000000" />
    </Border.Effect>
    <StackPanel Margin="0,4,0,8">
      <TextBlock
        x:Name="AppNameText"
        FontSize="13"
        FontWeight="SemiBold"
        Foreground="{DynamicResource SecondaryTextBrush}"
        Margin="20,16,20,4"
        TextTrimming="CharacterEllipsis"
      />
      <!-- Edit -->
      <Border Height="1" Background="{DynamicResource SeparatorBrush}" Margin="0,8,0,0" />
      <Button
        Click="OnEditClick"
        Style="{DynamicResource IconButtonStyle}"
        Padding="20,13"
        HorizontalContentAlignment="Left"
      >
        <TextBlock
          Text="{DynamicResource EditAppTitle}"
          FontSize="15"
          Foreground="{DynamicResource TitleTextBrush}"
        />
      </Button>
      <!-- Delete — кастомный красный hover, нет подходящего стиля в теме -->
      <Border Height="1" Background="{DynamicResource SeparatorBrush}" />
      <Button
        Click="OnDeleteClick"
        Background="Transparent"
        BorderThickness="0"
        Cursor="Hand"
        Padding="20,13"
        HorizontalContentAlignment="Left"
      >
        <Button.Template>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FFF0F0" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Button.Template>
        <TextBlock
          Text="{DynamicResource DeleteAppTitle}"
          FontSize="15"
          Foreground="{DynamicResource DangerBrush}"
        />
      </Button>
      <!-- Cancel -->
      <Border Height="1" Background="{DynamicResource SeparatorBrush}" />
      <Button
        Click="OnCancel"
        Style="{DynamicResource IconButtonStyle}"
        Padding="20,13"
        HorizontalContentAlignment="Left"
      >
        <TextBlock
          Text="{DynamicResource Cancel}"
          FontSize="15"
          Foreground="{DynamicResource SecondaryTextBrush}"
        />
      </Button>
    </StackPanel>
  </Border>
</Window>
