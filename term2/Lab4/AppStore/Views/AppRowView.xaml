<UserControl
  x:Class="Project.Views.AppRowView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
>
  <StackPanel>
    <Border Style="{DynamicResource AppRowStyle}" Padding="20,10" MouseLeftButtonUp="OnRowClick">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="52" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <!-- App icon -->
        <Border
          Grid.Column="0"
          Width="50"
          Height="50"
          CornerRadius="11"
          Background="{Binding Color, Converter={StaticResource ColorBrush}}"
          VerticalAlignment="Center"
        >
          <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="6" Opacity="0.12" Color="#000000" />
          </Border.Effect>
          <TextBlock
            Text="{Binding ShortName}"
            FontSize="12"
            FontWeight="Bold"
            Foreground="White"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            MaxWidth="44"
            TextTrimming="CharacterEllipsis"
            TextAlignment="Center"
          />
        </Border>
        <!-- Name + category -->
        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="14,0,12,0">
          <TextBlock
            Text="{Binding FullName}"
            Style="{DynamicResource AppNameStyle}"
            TextTrimming="CharacterEllipsis"
          />
          <TextBlock Text="{Binding Category}" Style="{DynamicResource AppCategoryStyle}" />
        </StackPanel>
        <!-- Get / price button — use ButtonLabel computed property -->
        <Button
          Grid.Column="2"
          VerticalAlignment="Center"
          Style="{DynamicResource GetButtonStyle}"
          Command="{Binding DownloadCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
          CommandParameter="{Binding}"
        >
          <TextBlock
            Foreground="{DynamicResource GetBtnTextBrush}"
            FontSize="13"
            FontWeight="SemiBold">
            <TextBlock.Text>
              <MultiBinding Converter="{StaticResource ButtonLabelConv}">
                <Binding Path="IsDownloaded" />
                <Binding Path="Price" />
                <Binding Path="DataContext.CurrentLanguage"
                  RelativeSource="{RelativeSource AncestorType=Window}" />
              </MultiBinding>
            </TextBlock.Text>
          </TextBlock>
        </Button>
      </Grid>
    </Border>
    <!-- Row separator (indented from icon) -->
    <Separator Style="{DynamicResource RowSeparatorStyle}" />
  </StackPanel>
</UserControl>