<Window
  x:Class="Project.Views.EditDeleteDialog"
  Icon="pack://application:,,,/Resources/Icons/app.ico"
  Cursor="pack://application:,,,/Resources/Cursors/arrow.cur"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Background="White"
  FontFamily="Segoe UI"
  Width="300"
  SizeToContent="Height"
  WindowStartupLocation="CenterOwner"
  ResizeMode="NoResize"
>
  <Border CornerRadius="14" Background="White" BorderThickness="0">
    <Border.Effect>
      <DropShadowEffect ShadowDepth="0" BlurRadius="28" Opacity="0.16" Color="#000000" />
    </Border.Effect>
    <StackPanel Margin="0,4,0,8">
      <!-- Title -->
      <TextBlock
        x:Name="AppNameText"
        FontSize="13"
        FontWeight="SemiBold"
        Foreground="#828282"
        Margin="20,16,20,4"
        TextTrimming="CharacterEllipsis"
      />
      <!-- Edit button -->
      <Border Height="1" Background="#F3F3F3" Margin="0,8,0,0" />
      <Button
        Click="OnEditClick"
        Background="Transparent"
        BorderThickness="0"
        Cursor="Hand"
        Padding="20,13"
        HorizontalContentAlignment="Left"
      >
        <Button.Template>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#F7F7F8" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Button.Template>
        <TextBlock Text="{DynamicResource EditAppTitle}" FontSize="15" Foreground="#272727" />
      </Button>
      <!-- Delete button -->
      <Border Height="1" Background="#F3F3F3" />
      <Button
        Click="OnDeleteClick"
        Background="Transparent"
        BorderThickness="0"
        Cursor="Hand"
        Padding="20,13"
        HorizontalContentAlignment="Left"
      >
        <Button.Template>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FFF0F0" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Button.Template>
        <TextBlock Text="{DynamicResource DeleteAppTitle}" FontSize="15" Foreground="#FF3B30" />
      </Button>
      <!-- Cancel -->
      <Border Height="1" Background="#F3F3F3" />
      <Button
        Click="OnCancel"
        Background="Transparent"
        BorderThickness="0"
        Cursor="Hand"
        Padding="20,13"
        HorizontalContentAlignment="Left"
      >
        <Button.Template>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#F7F7F8" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Button.Template>
        <TextBlock Text="{DynamicResource Cancel}" FontSize="15" Foreground="#828282" />
      </Button>
    </StackPanel>
  </Border>
</Window>
