<Window
  x:Class="Project.Views.ConfirmDialog"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Background="{DynamicResource WindowBgBrush}"
  FontFamily="Segoe UI"
  Width="320"
  SizeToContent="Height"
  WindowStartupLocation="CenterOwner"
  ResizeMode="CanResize"
>
  <Border CornerRadius="14" Background="{DynamicResource WindowBgBrush}" BorderThickness="0">
    <Border.Effect>
      <DropShadowEffect ShadowDepth="0" BlurRadius="30" Opacity="0.18" Color="#000000" />
    </Border.Effect>
    <StackPanel Margin="24,22,24,20">
      <TextBlock
        x:Name="TitleText"
        FontSize="15"
        FontWeight="SemiBold"
        Foreground="{DynamicResource TitleTextBrush}"
        TextWrapping="Wrap"
        Margin="0,0,0,8"
      />
      <TextBlock
        x:Name="MessageText"
        FontSize="13"
        Foreground="{DynamicResource SecondaryTextBrush}"
        TextWrapping="Wrap"
        Margin="0,0,0,20"
      />
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <!-- Cancel — GetButtonStyle: GetBtnBgBrush фон + GetBtnTextBrush цвет -->
        <Button
          x:Name="CancelBtn"
          Content="{DynamicResource Cancel}"
          Style="{DynamicResource GetButtonStyle}"
          MinWidth="80"
          Height="32"
          Margin="0,0,10,0"
          Click="OnCancel"
        />
        <!-- Confirm — PrimaryButtonStyle по умолчанию; code-behind меняет Background для danger-варианта -->
        <Button
          x:Name="ConfirmBtn"
          Style="{DynamicResource PrimaryButtonStyle}"
          MinWidth="80"
          Height="32"
          IsDefault="True"
          Click="OnConfirm"
        />
      </StackPanel>
    </StackPanel>
  </Border>
</Window>
